# Add your strings here, one per line
STRING1=example_username
STRING2=example_password
STRING3=another_string_to_type

# Optional settings
DELAY_BEFORE_START=5
DELAY_BETWEEN_STRINGS=1

# Key to press after each string (options: enter, tab, esc, none)
KEY_AFTER_STRING=enter 